version: '2.1'
services:
  seleniumhub:
      image: selenium/standalone-chrome:3.4.0
      hostname: seleniumhub
      environment:
          # Required to avoid container startup hanging sometimes in
          # some environments
          JAVA_OPTS: -Djava.security.egd=file:/dev/./urandom
      ports:
          - "4444:4444"

  testservice:
      container_name: testservice
      build:
        context: .
      links:
        - seleniumhub
      environment:
        -  WAGERPLAYER_APP_NAME=redbook
        -  WAGERPLAYER_BASE_URL=http://wp.luxaws.com/admin/frames.php
        -  WAGERPLAYER_MOBIV2_URL=http://wpapi.luxaws.com/mobi/v2/web
        -  WAGERPLAYER_USERNAME=admin
        -  WAGERPLAYER_PASSWORD=luxbet123
        -  WAGERPLAYER_RUN_MODE=DOCKER
        -  WAGERPLAYER_WAPI_URL=http://wpapi.luxaws.com/wapi1.php
        -  WAGERPLAYER_WAPI_PASSWORD=1e55afbd5ee7ed823f4351c63e4c4d39
        -  WAGERPLAYER_WAPI_USERNAME=testapi
        -  WAGERPLAYER_CUSTOMER_USERNAME=monika_01
        -  WAGERPLAYER_CUSTOMER_PASSWORD=luxbet321
        -  TEST_TAGS=${TEST_TAGS}